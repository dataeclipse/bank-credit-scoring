# Model Card — PD Scoring (кредитный скоринг)

> Скелет в духе банковской валидации модели (НБРК / Fed **SR 11-7**). Поля с `TODO`
> заполняются по мере прохождения фаз 1–5. Версионируется вместе с моделью.

| Поле | Значение |
|---|---|
| Модель | PD scoring (вероятность дефолта по кредитной заявке) |
| Версия модели | `TODO` (Фаза 2 — из MLflow registry) |
| Версия кода | pd-scoring `0.1.0` |
| Дата | `TODO` |
| Владелец / валидатор | `TODO` |
| Статус | Скелет (Фаза 0) |

## 1. Назначение и область применения (Purpose & Scope)
Оценка вероятности дефолта по кредитной заявке для поддержки решения о выдаче.
**In-scope:** `TODO`. **Out-of-scope:** автоматический отказ без участия человека `TODO`.

## 2. Данные
- Источник: **Home Credit Default Risk** (Kaggle, анонимизированные данные).
- Целевая переменная: `TARGET` (дефолт). Период / срез: `TODO`.
- Витрина: client-level, агрегации bureau/previous/installments **без утечек будущего** (Фаза 1).
- Качество данных, пропуски, репрезентативность: `TODO`.

## 3. Методология
- **Scorecard:** WOE/IV-биннинг (`optbinning`) + логистическая регрессия → баллы. `TODO`
- **GBDT:** LightGBM/CatBoost, подбор гиперпараметров. `TODO`
- Обоснование выбора модели в прод (качество ↔ интерпретируемость): `TODO` (Фаза 2).

## 4. Метрики качества
Holdout 61 503 заявок (Фаза 1 stratified split, seed 42). HPO — Optuna на подвыборке train,
финал — на полном train. Выбор в прод: **LightGBM** (scorecard — интерпретируемый challenger).

| Метрика | Scorecard | LightGBM | CatBoost |
|---|---|---|---|
| ROC-AUC | 0.770 | **0.790** | 0.789 |
| Gini | 0.539 | **0.579** | 0.579 |
| KS | 0.407 | **0.440** | 0.440 |
| PR-AUC | 0.255 | 0.287 | 0.289 |

Scorecard CV (биннинг внутри фолдов, без оптимизма): Gini 0.528. Детали:
[model_comparison.md](model_comparison.md), [model_selection.md](model_selection.md).

## 5. Калибровка
Метод (isotonic/Platt), reliability curve, Brier score до/после: `TODO` (Фаза 3).
Банку важна корректная вероятность PD, а не только ранжирование.

## 6. Объяснимость
Глобальная важность (SHAP) + локальные **reason codes** (топ-причины решения по заявке): `TODO` (Фаза 3).

## 7. Справедливость (Fairness)
Группы/прокси, метрики (disparate impact, equalized odds), митигция: `TODO` (Фаза 3, Fairlearn).

## 8. Ограничения и риски
Допущения, пределы применимости, известные слабые места, reject inference: `TODO`.

## 9. Требования к мониторингу
- **PSI/CSI** по входным фичам и дрейф распределения PD (Evidently). Пороги алертов: `TODO` (Фаза 4).
- Стабильность скорборда, периодичность ревалидации: `TODO`.

## 10. Governance / версионирование
Реестр моделей (MLflow), процесс ревалидации, аппрув, аудит-трейл: `TODO` (Фаза 5).
